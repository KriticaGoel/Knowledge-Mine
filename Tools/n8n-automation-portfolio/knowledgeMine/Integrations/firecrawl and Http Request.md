# Firecrawl
> Firecrawl is a Web Data API built specifically for AI applications and AI agents. Instead of manually writing web scrapers, handling JavaScript rendering, CAPTCHAs, dynamic pages, and cleaning HTML, Firecrawl provides a single API that turns web content into AI-friendly data.

https://www.firecrawl.dev/

Step 1: Sign up tp FireCrawl and copy the api key.

Step 2: Add as a next action add url and api key

Step 3: set query and source of search like web


# Http request

Ysed to connect http request in iiput we can send data and add the url on which we need to send the data

